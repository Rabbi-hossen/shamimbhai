package day_7;
import java.util.Scanner;
public class BinaryToDecemal {
    public static void main(String[] args) {
     /*   //binary :11000
        //decemal :32  (2^4 *1)+(2^3*1)+(2^2*0)+)+(2^1*0))+(2^0*0)----ans
        //              ex * ld
        System.out.println("input a binary number to convert in decemal :");
        Scanner sc = new Scanner(System.in);
        int j = sc.nextInt();

        static void BTD(int j) {
        int binary = j;
        int ans = 0;
        int exponent = 1;
        while (binary > 0) {
            int last_digit = (binary % 10);
            ans = ans + (exponent * last_digit);
            binary = binary / 2;
        }
        System.out.println(ans);
        }*/

    }
}
